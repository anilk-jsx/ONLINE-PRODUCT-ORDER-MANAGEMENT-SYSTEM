import { Component } from '@angular/core'; import { CommonModule } from '@angular/common'; import { FormsModule } from '@angular/forms'; import { StoreService, OrderStatus } from '../store.service';
@Component({ standalone:true, imports:[CommonModule,FormsModule], template:`
<div class="toolbar"><div><div class="eyebrow">{{store.isAdmin() ? 'Admin Orders' : 'Customer Orders'}}</div><h1 class="page-title">{{store.isAdmin() ? 'Order Management' : 'My Orders'}}</h1><p class="subtle mb-0">{{store.isAdmin() ? 'Update booking status and monitor every customer order.' : 'Track the orders you placed from the product catalogue.'}}</p></div></div>
<div class="panel table-responsive"><table class="table align-middle"><thead><tr><th>Order</th><th>Customer</th><th>Status</th><th>Total</th><th>Delivery</th></tr></thead><tbody><tr *ngFor="let o of store.visibleOrders()"><td><strong>{{o.orderNo}}</strong><div class="small subtle">{{o.createdAt}}</div></td><td>{{o.customer}}</td><td><select *ngIf="store.isAdmin(); else statusText" class="form-select form-select-sm" [(ngModel)]="o.status" (change)="store.updateOrder(o.id, o.status)"><option *ngFor="let s of statuses" [value]="s">{{s}}</option></select><ng-template #statusText><span class="badge text-bg-info">{{o.status}}</span></ng-template></td><td class="price">{{store.format(o.total)}}</td><td>{{o.deliveryPartner}}<div class="small subtle">{{o.trackingNo}}</div></td></tr></tbody></table></div>` })
export class OrdersComponent { statuses:OrderStatus[]=['PENDING','CONFIRMED','PACKED','OUT_FOR_DELIVERY','DELIVERED','CANCELLED']; constructor(public store:StoreService){} }
