import { Component } from '@angular/core'; import { CommonModule } from '@angular/common'; import { FormsModule } from '@angular/forms'; import { StoreService } from '../store.service';
@Component({ standalone:true, imports:[CommonModule,FormsModule], template:`
<div class="toolbar"><div><div class="eyebrow">Logistics</div><h1 class="page-title">Delivery Management</h1><p class="subtle mb-0">Assign courier partner, tracking number and expected delivery date.</p></div></div>
<div class="panel table-responsive"><table class="table align-middle"><thead><tr><th>Order</th><th>Customer</th><th>Partner</th><th>Tracking</th><th>Expected</th><th></th></tr></thead><tbody><tr *ngFor="let o of store.state.orders"><td>{{o.orderNo}}</td><td>{{o.customer}}</td><td><input class="form-control form-control-sm" [(ngModel)]="o.deliveryPartner"></td><td><input class="form-control form-control-sm" [(ngModel)]="o.trackingNo"></td><td><input class="form-control form-control-sm" [(ngModel)]="o.expectedDate"></td><td><button class="btn btn-sm btn-primary" (click)="save(o)">Save</button></td></tr></tbody></table></div>` })
export class DeliveryComponent { constructor(public store:StoreService){} save(o:any){this.store.updateDelivery(o.id,o.deliveryPartner,o.trackingNo,o.expectedDate);} }
